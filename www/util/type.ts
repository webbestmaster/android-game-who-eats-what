export type SizeType = {
    height: number;
    width: number;
};

export type PlaceType = {
    height: number;
    left: number;
    top: number;
    width: number;
};

export enum EnableDisableEnum {
    disable = 'disable',
    enable = 'enable',
}
